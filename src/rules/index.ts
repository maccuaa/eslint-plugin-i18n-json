import identicalKeys from "./identical-keys";
import sortedKeys from "./sorted-keys";
import validJson from "./valid-json";

export default {
  "valid-json": validJson,
  "identical-keys": identicalKeys,
  "sorted-keys": sortedKeys,
};
